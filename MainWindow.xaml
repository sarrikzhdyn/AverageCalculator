<Window x:Class="PasswordManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Менеджер паролей" Height="520" Width="880" WindowStartupLocation="CenterScreen">
    <!-- Ресурсы окна: стили и цвета -->
    <Window.Resources>
        <!-- Цветовая палитра -->
        <SolidColorBrush x:Key="BackgroundColor" Color="#E6F0FA"/>
        <!-- Нежно-голубой фон -->
        <SolidColorBrush x:Key="ButtonBackground" Color="#4A90E2"/>
        <!-- Голубой акцент -->
        <SolidColorBrush x:Key="ButtonHover" Color="#357ABD"/>
        <!-- Темнее при наведении -->
        <SolidColorBrush x:Key="TextColor" Color="#2C3E50"/>
        <!-- Темно-серый текст -->
        <SolidColorBrush x:Key="DisabledColor" Color="#B0B7C3"/>
        <!-- Серый для отключенных элементов -->

        <!-- Стиль для кнопок -->
        <Style TargetType="Button" x:Key="MinimalButtonStyle">
            <Setter Property="Background" Value="{StaticResource ButtonBackground}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource ButtonHover}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{StaticResource DisabledColor}"/>
                    <Setter Property="Foreground" Value="#D3D8E0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- Применение фона ко всему окну -->
    <Grid Background="{StaticResource BackgroundColor}" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель мастер-пароля -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Мастер-пароль:" VerticalAlignment="Center" Foreground="{StaticResource TextColor}"/>
            <PasswordBox x:Name="MasterPassBox" Width="260" Margin="6,0,0,0" PasswordChanged="MasterPassBox_PasswordChanged"/>
            <Button Content="Открыть" Width="100" Margin="10,0,0,0" Command="{Binding UnlockCommand}" Style="{StaticResource MinimalButtonStyle}"/>
            <Button Content="Сохранить" Width="100" Margin="8,0,0,0" Command="{Binding SaveCommand}" IsEnabled="{Binding IsUnlocked}" Style="{StaticResource MinimalButtonStyle}"/>
        </StackPanel>

        <!-- Таблица паролей -->
        <DataGrid x:Name="PasswordGrid" Grid.Row="1" ItemsSource="{Binding Credentials}" AutoGenerateColumns="False" CanUserAddRows="True" IsReadOnly="False" Margin="0,0,0,10" IsEnabled="{Binding IsUnlocked}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="3*"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="3*"/>
                <DataGridTextColumn Header="Пароль" Binding="{Binding Password}" Width="3*"/>
            </DataGrid.Columns>
            <DataGrid.Background>
                <SolidColorBrush Color="#FFFFFF"/>
                <!-- Белый фон таблицы для контраста -->
            </DataGrid.Background>
        </DataGrid>

        <!-- Панель кнопок действий -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Добавить" Width="100" Margin="0,0,10,0" Command="{Binding AddCommand}" IsEnabled="{Binding IsUnlocked}" Style="{StaticResource MinimalButtonStyle}"/>
            <Button Content="Сгенерировать пароль" Width="160" Margin="0,0,10,0" Command="{Binding GeneratePasswordCommand}" CommandParameter="{Binding SelectedItem, ElementName=PasswordGrid}" IsEnabled="{Binding IsUnlocked}" Style="{StaticResource MinimalButtonStyle}"/>
            <Button Content="Удалить выбранную" Width="140" Command="{Binding RemoveCommand}" CommandParameter="{Binding SelectedItem, ElementName=PasswordGrid}" IsEnabled="{Binding IsUnlocked}" Style="{StaticResource MinimalButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
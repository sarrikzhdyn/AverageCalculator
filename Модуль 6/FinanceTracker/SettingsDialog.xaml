<Window x:Class="FinanceTracker.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinanceTracker"
        mc:Ignorable="d"
        Title="SettingsDialog" Height="450" Width="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="URL базы данных Firebase:" Margin="0,0,0,10"/>
        <TextBox Grid.Row="1" x:Name="txtFirebaseUrl" Height="25" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="2" Text="Пример: https://your-project.firebaseio.com/" 
                   FontStyle="Italic" Foreground="Gray"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnOk" Content="Сохранить" Click="BtnOk_Click" 
                    Padding="15,5" Margin="0,0,10,0" IsDefault="True"/>
            <Button x:Name="btnCancel" Content="Отмена" Click="BtnCancel_Click" 
                    Padding="15,5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
